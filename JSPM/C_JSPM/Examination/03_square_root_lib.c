#include <stdio.h>
#include <math.h>

int main()
{
    int num = 9;
    printf("The Square Root of %d is %f\n", num, sqrt(num));
    printf("The Square Root of %d is %.2f\n", num, sqrt(num));
    return 0;
}